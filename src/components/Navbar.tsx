const Navbar = () => {
  return (
    <div className="navbar">
      <div className="nav-title">💰 FinCalc</div>
      <div className="nav-buttons">
        <button>Home</button>
        <button>Dashboard</button>
        <button>Settings</button>
        <button>Profile</button>
      </div>
    </div>
  );
};

export default Navbar;
